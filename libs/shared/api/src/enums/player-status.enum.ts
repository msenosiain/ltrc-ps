export enum PlayerStatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum PlayerAvailabilityEnum {
  AVAILABLE = 'available',
  INJURED = 'injured',
  CALLED_UP = 'called_up',
  SUSPENDED = 'suspended',
  LEAVE = 'leave',
}
