export enum ExerciseTrackingTypeEnum {
  WEIGHT_REPS = 'weight_reps',           // Press de banca, sentadilla
  WEIGHT_REPS_EACH = 'weight_reps_each', // Curl bíceps, press hombro mancuernas
  BODYWEIGHT_REPS = 'bodyweight_reps',   // Flexiones
  BODYWEIGHT_LOADED = 'bodyweight_loaded', // Dominadas (peso corporal + carga extra)
  ASSISTED = 'assisted',                 // Dominadas asistidas
  TIME = 'time',                         // Plancha
  TIME_EACH = 'time_each',               // Plancha lateral
  DISTANCE = 'distance',                 // Correr, remo
}
