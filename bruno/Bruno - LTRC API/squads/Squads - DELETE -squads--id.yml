info:
  name: Squads - DELETE /squads/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/squads/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
