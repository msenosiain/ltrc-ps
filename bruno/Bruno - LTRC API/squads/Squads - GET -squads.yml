info:
  name: Squads - GET /squads
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/squads"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
