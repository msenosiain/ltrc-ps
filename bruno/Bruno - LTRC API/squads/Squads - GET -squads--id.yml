info:
  name: Squads - GET /squads/:id
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/squads/:id'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
