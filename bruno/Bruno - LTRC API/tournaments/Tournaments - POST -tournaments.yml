info:
  name: Tournaments - POST /tournaments
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/tournaments"
  body:
    type: json
    data: |-
      {
        "name": "Torneo Regional Cuyano",
        "season": "2026",
        "description": "Torneo regional organizado por la URC"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
