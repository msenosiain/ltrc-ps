info:
  name: Tournaments - GET /tournaments/:id
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/tournaments/:id'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Reemplazar :id por el id del torneo devuelto al crear
