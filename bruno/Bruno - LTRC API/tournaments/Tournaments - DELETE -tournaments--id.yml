info:
  name: Tournaments - DELETE /tournaments/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/tournaments/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Reemplazar :id por el id del torneo
