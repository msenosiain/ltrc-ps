info:
  name: Tournaments - GET /tournaments
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/tournaments'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
