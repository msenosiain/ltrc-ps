info:
  name: Players - DELETE /players/:id
  type: http
  seq: 1

http:
  method: DELETE
  url: '{{baseUrl}}/players/:id'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
