info:
  name: Players - GET /players/:id
  type: http
  seq: 1

http:
  method: GET
  url: '{{baseUrl}}/players/:id'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Reemplazar :id por el _id del jugador devuelto al crear
