meta {
  name: Participants - PATCH update status
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/trips/{{tripId}}/participants/{{participantId}}
  body: json
  auth: inherit
}

body:json {
  {
    "status": "confirmed"
  }
}
