info:
  name: Health Mongo - GET /health/mongo
  type: http
  seq: 2

http:
  method: GET
  url: '{{baseUrl}}/health/mongo'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
