info:
  name: Matches - GET /matches/:id
  type: http
  seq: 3

http:
  method: GET
  url: '{{baseUrl}}/matches/:id'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Retorna el partido con tournament y selectedPlayers populados.
  Reemplazar :id por el id del partido.
